# Example 1: A basic hello world.

 # a comment

# global data
int i;
int target;
gpp_t index_array;
gpp_t data_array;

# header functions
void f(int *target, gpp_t index_array, gpp_t data_array)
{
	fprintf(stdout, "Hello World!\n");
	*target = 0;
}

# pil program
node(1, i, [1:1:1], target, [0], f(&target, index_array, data_array))

